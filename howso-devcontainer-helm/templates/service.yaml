apiVersion: v1
kind: Service
metadata:
  name: {{ include "howso-devcontainer.fullname" . }}
  labels:
    {{- include "howso-devcontainer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "howso-devcontainer.selectorLabels" . | nindent 4 }}
